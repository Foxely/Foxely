class Fib {
    get(n) {
      if (n < 2) return n;
      return this.get(n - 2) + this.get(n - 1);
    }
}

var start = clock();
var fib = Fib();

for (var i = 0; i < 100; i = i + 1) {
    print fib.get(28);
}

print clock() - start;