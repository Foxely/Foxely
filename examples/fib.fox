
import "core";

start := clock();

fibonacci :: func(n) {
  if (n <= 1)
    return n;
  return fibonacci(n - 2) + fibonacci(n - 1);
}

for (i := 0; i <= 35; ++i)
{
    print "%\n", fibonacci(i);
}

print "Time: %\n", clock() - start;