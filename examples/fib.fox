
import "core";

start := clock();

fibonacci :: func(n) {
  if (n < 2)
    return n;
  return fibonacci(n - 2) + fibonacci(n - 1);
}

    fibonacci(35);
// for (i := 0; i <= 1; ++i)
// {
// }

print "Time: %\n", clock() - start;