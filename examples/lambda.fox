
myCustom :: func(value)
{
    print "value = %\n", value;
}

callable :: func(custom)
{
    custom(10);
}

callable(myCustom);

// This part is not working but this is how it will be work later
callable(func(value)
{
    print "value = %\n", value;
});