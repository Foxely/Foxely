#include <sys/time.h>


fibonacci :: func(n: int) : int
{
    if (n <= 1)
      return n;
    return fibonacci(n - 2) + fibonacci(n - 1);
}

main :: func(ac: int, av: **char) : int
{
    for (i: int = 0; i < 80; i = i + 1)
    {
        print "%d\\n", fibonacci(i);
    }

    return 0;
}